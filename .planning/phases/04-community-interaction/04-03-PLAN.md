---
phase: 04-community-interaction
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified: [src/app/gallery/page.tsx, src/components/Header.tsx]
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - 'User can browse a list of public stone slab designs'
    - 'Each gallery item shows a preview and basic details'
    - 'User can load a gallery design into the configurator'
  artifacts:
    - path: src/app/gallery/page.tsx
      provides: 'Community gallery UI'
    - path: src/components/Header.tsx
      provides: 'Navigation link to Gallery'
---

<objective>
Create a Community Gallery where users can discover public projects and use them as inspiration for their own designs.

Purpose: Showcase capabilities and inspire new users.
Output: Gallery page and navigation link.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/gallery/page.tsx
@src/components/Header.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Gallery Page</name>
  <files>src/app/gallery/page.tsx</files>
  <action>
    Create a new page that fetches all projects marked as `is_public`.
    Display them in a grid of cards.
    Each card should show the material name, item count, and a preview (if available).
    Add a "View in Studio" button to each card.
  </action>
  <verify>
    <automated>ls src/app/gallery/page.tsx</automated>
  </verify>
  <done>Gallery page exists and fetches data.</done>
</task>

<task type="auto">
  <name>Task 2: Update Navigation</name>
  <files>src/components/Header.tsx</files>
  <action>
    Add a "Galerija" (Gallery) link to the main navigation in the Header.
  </action>
  <verify>
    <automated>grep "Galerija" src/components/Header.tsx</automated>
  </verify>
  <done>Gallery link is visible in the header.</done>
</task>
</tasks>

<verification>
Manual verification: Navigate to /gallery. You should see a list of projects. Clicking one should redirect to the configurator with that project loaded.
</verification>

<success_criteria>

- [ ] Gallery displays public projects correctly.
- [ ] Users can navigate to the gallery from the header.
- [ ] Loading from gallery works seamlessly.
      </success_criteria>

<output>
After completion, create `.planning/phases/04-community-interaction/04-03-SUMMARY.md`
</output>
