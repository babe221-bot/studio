---
phase: 04-community-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/hooks/useVoiceCommands.ts, src/components/Lab.tsx]
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - 'User can activate voice recognition via a button'
    - "System recognizes commands like 'dužina 150'"
    - 'System provides visual feedback when a command is recognized'
  artifacts:
    - path: src/hooks/useVoiceCommands.ts
      provides: 'Voice command processing'
    - path: src/components/Lab.tsx
      provides: 'Microphone UI integration'
---

<objective>
Integrate voice commands into the configurator to allow hands-free dimensioning and basic operations.

Purpose: Improve accessibility and UX for professionals in the field.
Output: Functional microphone button in the sidebar with live feedback.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useVoiceCommands.ts
@src/components/Lab.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire Voice Handlers</name>
  <files>src/components/Lab.tsx</files>
  <action>
    Integrate `useVoiceCommands` into the `Lab` component.
    Map the handlers to `setLength`, `setWidth`, `setHeight`, `handleAddToOrder`, and `handleDownloadPdf`.
    Ensure the state updates correctly when commands are processed.
  </action>
  <verify>
    <automated>grep "useVoiceCommands" src/components/Lab.tsx</automated>
  </verify>
  <done>Hook is integrated and handlers are wired.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Voice UI</name>
  <files>src/components/Lab.tsx</files>
  <action>
    Add a floating or sidebar-integrated Microphone button.
    Show an animation or pulse when `isListening` is true.
    Display the recognized transcript in a small tooltip or toast for immediate feedback.
  </action>
  <verify>
    <automated>grep "Mic" src/components/Lab.tsx</automated>
  </verify>
  <done>Microphone button exists with visual feedback.</done>
</task>
</tasks>

<verification>
Manual verification: Click the microphone and say "dužina sto pedeset" (length 150). The length input should update.
</verification>

<success_criteria>

- [ ] Voice recognition starts/stops correctly.
- [ ] Specific dimension commands work (Length/Width/Height).
- [ ] Operations commands work (Add to order, PDF).
      </success_criteria>

<output>
After completion, create `.planning/phases/04-community-interaction/04-01-SUMMARY.md`
</output>
