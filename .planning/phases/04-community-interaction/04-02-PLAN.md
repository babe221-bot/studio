---
phase: 04-community-interaction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/hooks/useSupabasePersistence.ts, src/app/share/[token]/page.tsx, src/components/Lab.tsx]
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "User can generate a sharing link for a project"
    - "Shared link loads the project configuration for any user"
    - "Shared view is read-only (or creates a copy)"
  artifacts:
    - path: src/app/share/[token]/page.tsx
      provides: "Public project loading"
    - path: src/hooks/useSupabasePersistence.ts
      provides: "Token-based project fetching"
---

<objective>
Enable project sharing via unique tokens, allowing users to collaborate or show their designs to others.

Purpose: Foster collaboration and social discovery of designs.
Output: "Share" button in Lab UI and a functional `/share/[token]` route.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useSupabasePersistence.ts
@src/components/Lab.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Token-based Persistence</name>
  <files>src/hooks/useSupabasePersistence.ts</files>
  <action>
    Add a function `shareProject(id: string)` that generates a unique `share_token` (if not exists) and marks the project as public.
    Add a function `fetchSharedProject(token: string)` that allows anyone to retrieve a project by its token.
  </action>
  <verify>
    <automated>grep "shareProject" src/hooks/useSupabasePersistence.ts</automated>
  </verify>
  <done>Persistence layer supports sharing tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Create Share Route</name>
  <files>src/app/share/[token]/page.tsx</files>
  <action>
    Create a new page that accepts a token.
    Fetch the project via `fetchSharedProject`.
    Load the project items into the Lab state (or a specialized preview mode).
    Provide a "Remix in Studio" button to let the viewer start editing.
  </action>
  <verify>
    <automated>ls src/app/share/[token]/page.tsx</automated>
  </verify>
  <done>Sharing route is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add Share UI</name>
  <files>src/components/Lab.tsx</files>
  <action>
    Add a "Podijeli" (Share) button in the Radni nalog header.
    When clicked, it should call `shareProject` and show a "Copy Link" modal with the `/share/[token]` URL.
  </action>
  <verify>
    <automated>grep "Podijeli" src/components/Lab.tsx</automated>
  </verify>
  <done>UI for sharing exists in the configurator.</done>
</task>
</tasks>

<verification>
Manual verification: Save a version, click Share, copy the link, and open it in an Incognito window. The design should load correctly.
</verification>

<success_criteria>

- [ ] Share token is generated and persisted.
- [ ] Anyone with the link can view the configuration.
- [ ] User can copy the link to clipboard.
      </success_criteria>

<output>
After completion, create `.planning/phases/04-community-interaction/04-02-SUMMARY.md`
</output>
