---
phase: 03-commerce-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  [
    src/types/index.ts,
    src/hooks/useOrderCalculations.ts,
    src/components/Lab.tsx,
    src/components/three/StoneSlabMesh.tsx,
  ]
autonomous: true
requirements: [FEAT-02]
must_haves:
  truths:
    - 'User can shift the stone texture (grain) X and Y directions'
    - 'Texture offset is visible in the 3D viewer'
    - 'Texture offset is saved with the order item'
  artifacts:
    - path: src/components/three/StoneSlabMesh.tsx
      provides: 'Texture offset application'
      pattern: 'texture.offset.set'
    - path: src/components/Lab.tsx
      provides: 'Grain alignment UI'
---

<objective>
Implement a Grain Alignment tool that allows users to shift the stone texture on the slab for better visual matching (bookmatching).

Purpose: Provide precision control over the stone's appearance.
Output: UI controls for texture offset and updated 3D rendering logic.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/index.ts
@src/components/three/StoneSlabMesh.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend Data Model</name>
  <files>src/types/index.ts, src/hooks/useOrderCalculations.ts</files>
  <action>
    Add `textureOffset: { x: number, y: number }` to `OrderItem` and `CalculationParams`.
    Ensure default is `{ x: 0, y: 0 }`.
  </action>
  <verify>
    <automated>grep "textureOffset" src/types/index.ts</automated>
  </verify>
  <done>Data types updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Alignment UI</name>
  <files>src/components/Lab.tsx</files>
  <action>
    Add a new section "Orijentacija teksture" in the sidebar of `Lab.tsx`.
    Include four directional buttons or two sliders (X and Y) to adjust `textureOffset`.
    Update the state when buttons are clicked.
  </action>
  <verify>
    <automated>grep "Orijentacija teksture" src/components/Lab.tsx</automated>
  </verify>
  <done>UI controls added to sidebar.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Offset to 3D Mesh</name>
  <files>src/components/three/StoneSlabMesh.tsx</files>
  <action>
    Update `StoneSlabMesh.tsx` to receive `textureOffset`.
    Apply the offset to all loaded textures (diffuse, normal, roughness) using `texture.offset.set(x, y)`.
    Ensure `texture.wrapS` and `texture.wrapT` are set to `THREE.RepeatWrapping`.
  </action>
  <verify>
    <automated>grep "offset.set" src/components/three/StoneSlabMesh.tsx</automated>
  </verify>
  <done>3D rendering logic updated to respect offsets.</done>
</task>
</tasks>

<verification>
Manual verification in the 3D viewer: moving sliders/buttons should shift the stone's grain.
</verification>

<success_criteria>

- [ ] `textureOffset` state is correctly managed.
- [ ] UI controls are present and functional.
- [ ] 3D viewer reflects texture shifts in real-time.
      </success_criteria>

<output>
After completion, create `.planning/phases/03-commerce-configuration/03-02-SUMMARY.md`
</output>
