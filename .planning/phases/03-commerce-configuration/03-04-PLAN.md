---
phase: 03-commerce-configuration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  [
    src/hooks/useSupabasePersistence.ts,
    src/components/Lab.tsx,
    src/lib/supabase.ts,
  ]
autonomous: true
requirements: [FEAT-04]
---

<objective>
Migrate project persistence from LocalStorage to Supabase to enable multi-slab project synchronization and user accounts.

Purpose: Provide a reliable, cloud-synced project storage.
Output: `useSupabasePersistence` hook and updated `Lab.tsx` integration.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useProjectHistory.ts
@src/lib/supabase.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Supabase Persistence Hook</name>
  <files>src/hooks/useSupabasePersistence.ts</files>
  <action>
    Create `src/hooks/useSupabasePersistence.ts`.
    Implement functions to:
    1. Fetch all projects for the current user.
    2. Save a project (including `items` array as JSON or in a separate table).
    3. Delete a project.
    Handle loading and error states.
    (Note: If the user is not logged in, fallback to anonymous persistence or prompt for login).
  </action>
  <verify>
    <automated>ls src/hooks/useSupabasePersistence.ts</automated>
  </verify>
  <done>Hook implemented with basic project CRUD.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into Lab UI</name>
  <files>src/components/Lab.tsx</files>
  <action>
    Replace the usage of `useProjectHistory` with `useSupabasePersistence`.
    Ensure "Spremi verziju" and "Spremi predložak" now call the Supabase hook.
    Update the `VersionHistoryDialog` and `TemplateManager` to use the synced data.
  </action>
  <verify>
    <automated>grep "useSupabasePersistence" src/components/Lab.tsx</automated>
  </verify>
  <done>Lab UI is backed by Supabase.</done>
</task>
</tasks>

<verification>
Manual verification: Saving a project should appear in the Supabase Dashboard `projects` table.
</verification>

<success_criteria>

- [ ] Projects are saved to Supabase instead of LocalStorage.
- [ ] Multi-slab project data (all items) is preserved.
- [ ] UI correctly displays cloud-synced projects.
      </success_criteria>

<output>
After completion, create `.planning/phases/03-commerce-configuration/03-04-SUMMARY.md`
</output>
