---
phase: 03-commerce-configuration
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified: [src/lib/pdf.ts, src/components/Lab.tsx, src/types/index.ts]
autonomous: true
requirements: [FEAT-03]
---

<objective>
Enhance the PDF generation to create professional-grade quotations including technical drawings, 3D renders, and itemized costs.

Purpose: Provide customers with a high-quality document for their project planning and purchase.
Output: Branded PDF quotation with CAD drawings and detailed breakdowns.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/pdf.ts
@src/ai/flows/imageGenerationFlow.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Capture Technical Drawings</name>
  <files>src/components/Lab.tsx, src/types/index.ts</files>
  <action>
    Modify `Lab.tsx` to trigger `generateTechnicalDrawing` (from `@/ai/flows/imageGenerationFlow.ts`) when "Dodaj u nalog" or "Spremi" is clicked.
    Store the resulting `imageUrl` or `imageDataUri` in the `OrderItem`.
    Ensure the drawing includes correct dimensions and edge processing.
  </action>
  <verify>
    <automated>grep "generateTechnicalDrawing" src/components/Lab.tsx</automated>
  </verify>
  <done>Technical drawings are captured and stored with order items.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance PDF Generation</name>
  <files>src/lib/pdf.ts</files>
  <action>
    Update `pdf.ts` (or the relevant PDF generation logic) to:
    1. Add a professional company logo and header.
    2. Embed the technical drawing images for each item.
    3. Include a detailed table of costs (Material, Processing, Okapnik, Total).
    4. Add a section for "Napomene" (Notes).
  </action>
  <verify>
    <automated>grep "addImage" src/lib/pdf.ts</automated>
  </verify>
  <done>PDF generator updated with images and detailed tables.</done>
</task>
</tasks>

<verification>
Manual verification: Generate a PDF for an order and check if drawings and costs are correctly displayed and formatted.
</verification>

<success_criteria>

- [ ] PDF contains technical drawings for each item.
- [ ] Costs are itemized in a table.
- [ ] Layout is professional and branded.
      </success_criteria>

<output>
After completion, create `.planning/phases/03-commerce-configuration/03-03-SUMMARY.md`
</output>
