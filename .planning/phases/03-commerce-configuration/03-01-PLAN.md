---
phase: 03-commerce-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [
    package.json,
    src/app/api/checkout/route.ts,
    src/components/Lab.tsx,
    src/app/checkout/success/page.tsx,
    src/app/checkout/cancel/page.tsx,
  ]
autonomous: true
requirements: [FEAT-01]
user_setup:
  - service: stripe
    why: 'Payment processing'
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: 'Stripe Dashboard -> Developers -> API keys'
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: 'Stripe Dashboard -> Developers -> API keys'
---

<objective>
Implement Stripe Payments integration to allow users to pay a deposit for their custom stone projects.

Purpose: Enable commercial transactions directly through the configurator.
Output: Working Stripe checkout flow with success/cancel pages.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/components/Lab.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Stripe Backend Setup</name>
  <files>package.json, src/app/api/checkout/route.ts, .env.example</files>
  <action>
    Add `stripe` to `package.json`.
    Create `src/app/api/checkout/route.ts`.
    Implement a POST handler that:
    1. Validates the request body (order items, total amount).
    2. Creates a Stripe Checkout Session with line items.
    3. Returns the session URL.
    Add `STRIPE_SECRET_KEY` and `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` to `.env.example`.
  </action>
  <verify>
    <automated>npm list stripe</automated>
  </verify>
  <done>Stripe dependency added and API route implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Stripe Client Integration</name>
  <files>package.json, src/components/Lab.tsx, src/app/checkout/success/page.tsx, src/app/checkout/cancel/page.tsx</files>
  <action>
    Add `@stripe/stripe-js` to `package.json`.
    In `Lab.tsx` (specifically `CalculationSummary`), add a "Plati depozit" (Pay Deposit) button.
    Implement the click handler to call `/api/checkout` and redirect to the Stripe session URL.
    Create simple success and cancel pages in `src/app/checkout/success/page.tsx` and `src/app/checkout/cancel/page.tsx`.
  </action>
  <verify>
    <automated>npm list @stripe/stripe-js</automated>
  </verify>
  <done>Checkout button added and redirect pages created.</done>
</task>
</tasks>

<verification>
Manual verification of the checkout flow (requires Stripe Test mode keys).
</verification>

<success_criteria>

- [ ] Stripe dependencies installed.
- [ ] API route `/api/checkout` returns a valid session.
- [ ] "Pay Deposit" button redirects to Stripe.
- [ ] Success/Cancel pages are accessible.
      </success_criteria>

<output>
After completion, create `.planning/phases/03-commerce-configuration/03-01-SUMMARY.md`
</output>
