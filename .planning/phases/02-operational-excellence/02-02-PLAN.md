---
phase: 02-operational-excellence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  [
    src/lib/calculations.ts,
    src/hooks/useOrderCalculations.ts,
    src/tests/calculations.test.ts,
    tests/e2e/checkout-journey.spec.ts,
  ]
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - 'Price calculations are testable without React'
    - 'Unit tests cover >80% of calculation logic'
    - 'E2E tests verify the checkout flow'
  artifacts:
    - path: src/lib/calculations.ts
      provides: 'Pure calculation logic'
      exports: ['calculateOrderCosts']
    - path: src/tests/calculations.test.ts
      provides: 'Unit tests for calculations'
      contains: "describe('calculateOrderCosts'"
  key_links:
    - from: 'src/hooks/useOrderCalculations.ts'
      to: 'src/lib/calculations.ts'
      via: 'import { calculateOrderCosts }'
    - from: 'src/tests/calculations.test.ts'
      to: 'src/lib/calculations.ts'
      via: 'import'
---

<objective>
Refactor price calculation logic into a pure library function for better testability and improved unit test coverage.

Purpose: Isolate business logic from UI state, enabling robust unit testing.
Output: `src/lib/calculations.ts` with >80% test coverage and updated hook.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useOrderCalculations.ts
@src/tests/useOrderCalculations.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract Calculation Logic</name>
  <files>src/lib/calculations.ts, src/tests/calculations.test.ts</files>
  <behavior>
    - should calculate correct costs for piece unit
    - should calculate correct costs for sqm unit
    - should calculate correct costs for lm unit
    - should handle missing material/finish gracefully (return 0)
    - should include okapnik and bunja costs correctly
  </behavior>
  <action>
    Create `src/lib/calculations.ts`.
    Extract the core calculation logic from `useOrderCalculations.ts` into a pure function `calculateOrderCosts` (or similar).
    Ensure it accepts a plain object of params (not hook args).
    Write comprehensive unit tests in `src/tests/calculations.test.ts` covering edge cases.
  </action>
  <verify>
    <automated>npm test src/tests/calculations.test.ts</automated>
  </verify>
  <done>Logic extracted and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate & Verify Hook</name>
  <files>src/hooks/useOrderCalculations.ts, src/tests/useOrderCalculations.test.ts</files>
  <action>
    Update `src/hooks/useOrderCalculations.ts` to use the new `calculateOrderCosts` function from `src/lib/calculations.ts`.
    Keep the hook as a wrapper that manages memoization.
    Update existing `src/tests/useOrderCalculations.test.ts` to ensure no regression (or delete if redundant, but better to keep as integration test of the hook).
  </action>
  <verify>
    <automated>npm test src/tests/useOrderCalculations.test.ts</automated>
  </verify>
  <done>Hook uses new lib and tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify E2E Coverage</name>
  <files>tests/e2e/checkout-journey.spec.ts</files>
  <action>
    Review `tests/e2e/checkout-journey.spec.ts`.
    Ensure it explicitly verifies the calculated price appears on the screen during the flow.
    If not, add an assertion for the price element.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/checkout-journey.spec.ts</automated>
  </verify>
  <done>E2E test confirms price visibility.</done>
</task>
</tasks>

<verification>
Run all tests: `npm test` and `npx playwright test`.
</verification>

<success_criteria>

- [ ] `src/lib/calculations.ts` exists and is pure.
- [ ] Unit tests pass with high coverage.
- [ ] `useOrderCalculations` hook still works.
- [ ] E2E tests pass.
      </success_criteria>

<output>
After completion, create `.planning/phases/02-operational-excellence/02-02-SUMMARY.md`
</output>
