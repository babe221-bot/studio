---
phase: 02-operational-excellence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  [
    tsconfig.json,
    src/components/AIAssistant.tsx,
    src/components/three/StoneSlabMesh.tsx,
    src/components/Lab.tsx,
    eslint.config.mjs,
  ]
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "No explicit 'any' types in AIAssistant.tsx"
    - 'TypeScript compiles without errors'
    - 'Linting rules are consistent'
  artifacts:
    - path: src/components/AIAssistant.tsx
      provides: 'AI Assistant UI'
      contains: 'interface'
    - path: tsconfig.json
      provides: 'TS Config'
      contains: '"strict": true'
  key_links:
    - from: 'src/components/AIAssistant.tsx'
      to: 'ai-sdk'
      via: 'types'
---

<objective>
Improve code quality by enforcing TypeScript strictness and removing `any` types from critical components.

Purpose: Reduce runtime errors and improve maintainability.
Output: Strictly typed `AIAssistant.tsx` and standardized linting config.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@tsconfig.json
@src/components/AIAssistant.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fix Types in AIAssistant</name>
  <files>src/components/AIAssistant.tsx</files>
  <action>
    Replace `any` types with proper interfaces in `src/components/AIAssistant.tsx`.
    Define interfaces for the AI message parts and props if missing.
    Use `ai-sdk` types where appropriate (e.g., `Message`, `UseChatHelpers`).
  </action>
  <verify>
    <automated>grep "any" src/components/AIAssistant.tsx | wc -l && tsc --noEmit</automated>
  </verify>
  <done>No `any` usage in AIAssistant.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize Linting & Formatting</name>
  <files>eslint.config.mjs, .prettierrc (if exists)</files>
  <action>
    Ensure `eslint.config.mjs` includes recommended rules for React and TypeScript.
    Run `npm run lint` and fix auto-fixable issues.
    (If `prettier` config is missing, create it or ensure defaults are good).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Linting passes.</done>
</task>

<task type="auto">
  <name>Task 3: Address Other Critical Any Types (Time Permitting)</name>
  <files>src/components/three/StoneSlabMesh.tsx, src/components/Lab.tsx</files>
  <action>
    If AIAssistant is clean, attempt to fix `any` types in `StoneSlabMesh.tsx` (THREE.js types) and `Lab.tsx` (props).
    Do NOT break functionality. If too complex, leave for future, but prioritize `AIAssistant`.
  </action>
  <verify>
    <automated>tsc --noEmit</automated>
  </verify>
  <done>Reduced `any` count in codebase.</done>
</task>
</tasks>

<verification>
Run `tsc --noEmit` to ensure no new errors.
Run `npm run lint` to ensure style consistency.
</verification>

<success_criteria>

- [ ] `AIAssistant.tsx` is strictly typed.
- [ ] TypeScript build passes.
- [ ] Linter passes.
      </success_criteria>

<output>
After completion, create `.planning/phases/02-operational-excellence/02-03-SUMMARY.md`
</output>
