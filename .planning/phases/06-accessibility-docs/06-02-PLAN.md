---
phase: 06-accessibility-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Lab.tsx, src/components/ui, src/hooks, docs]
autonomous: true
requirements: [UX-04, OPS-07]
must_haves:
  truths:
    - 'Configurator passes WCAG 2.1 AA compliance'
    - 'All interactive elements are keyboard accessible'
    - 'Screen reader announces dimension changes and actions'
  artifacts:
    - path: src/components/Lab.tsx
      provides: 'ARIA labels and keyboard navigation'
    - path: docs/onboarding.md
      provides: 'User onboarding documentation'
---

<objective>
Implement accessibility improvements and create onboarding documentation.

Purpose: Ensure the platform is usable by people with disabilities and new users can quickly learn the system.
Output: WCAG-compliant UI and comprehensive documentation.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Lab.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add ARIA Labels and Roles</name>
  <files>src/components/Lab.tsx</files>
  <action>
    1. Add aria-label to all buttons and interactive elements
    - Microphone button, dimension inputs, Add to Order, Download PDF
    2. Add aria-live regions for dynamic content (dimension changes, voice feedback)
    3. Add role="slider" for dimension inputs with proper aria-valuenow
  </action>
  <verify>
    <automated>grep "aria-" src/components/Lab.tsx</automated>
  </verify>
  <done>All interactive elements have proper ARIA attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Keyboard Navigation</name>
  <files>src/components/Lab.tsx, src/hooks</files>
  <action>
    1. Ensure Tab order follows logical flow
    - Dimensions → Stone selector → Actions
    2. Add keyboard shortcuts:
    - Arrow keys for dimension increment/decrement
    - Enter to confirm, Escape to cancel
    - Ctrl+S to save, Ctrl+P for PDF
    3. Add visible focus indicators
  </action>
  <verify>
    <automated>grep "onKeyDown" src/components/Lab.tsx</automated>
  </verify>
  <done>All features accessible via keyboard only.</done>
</task>

<task type="auto">
  <name>Task 3: Add Color Contrast and Visual Accessibility</name>
  <files>src/components/ui</files>
  <action>
    1. Audit color contrast ratios (minimum 4.5:1 for text)
    2. Add high contrast mode toggle
    3. Ensure error states are not color-only (add icons)
    4. Add reduce motion preference support for animations
  </action>
  <verify>
    <automated>grep "prefers-reduced-motion" src</automated>
  </verify>
  <done>UI meets WCAG color contrast requirements.</done>
</task>

<task type="auto">
  <name>Task 4: Create Onboarding Documentation</name>
  <files>docs/onboarding.md</files>
  <action>
    Create comprehensive onboarding docs:
    1. Quick start guide (5 minute setup)
    2. Configuration tutorial with screenshots
    3. Voice commands cheat sheet
    4. FAQ for common issues
    5. API integration guide for developers
  </action>
  <verify>
    <automated>ls docs/</automated>
  </verify>
  <done>Documentation covers all user personas.</done>
</task>
</tasks>

<verification>
1. Run axe-core accessibility audit
2. Test complete flow using only keyboard
3. Test with NVDA screen reader
4. Verify documentation renders correctly
</verification>

<success_criteria>

- [ ] All interactive elements have ARIA labels
- [ ] Keyboard navigation works for all features
- [ ] Color contrast meets WCAG AA
- [ ] Onboarding docs are complete and accurate
      </success_criteria>

<output>
After completion, create `.planning/phases/06-accessibility-docs/06-02-SUMMARY.md`
</output>
