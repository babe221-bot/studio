---
phase: 05-debugging-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [src/lib/WorkerPool.ts, src/workers/geometryWorker.ts, backend/app/api/cad.py]
autonomous: true
requirements: [DEV-04]
must_haves:
  truths:
    - 'Worker pool activity is visible in debug logs'
    - 'Geometry worker reports detailed generation metrics'
    - 'API endpoints log validation failures'
  artifacts:
    - path: src/lib/WorkerPool.ts
      provides: 'Diagnostic logging for workers'
    - path: src/workers/geometryWorker.ts
      provides: 'Tracing for mesh generation'
---

<objective>
Implement comprehensive diagnostic logging and tracing across the Web Worker Pool, Geometry Worker, and Backend API to identify stability issues and performance bottlenecks.

Purpose: Provide data for Source A (Worker Pool) and Source B (CAD Processing) troubleshooting.
Output: Instrumented codebase with structured debug logging.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@plans/debugging-testing-plan.md
@src/lib/WorkerPool.ts
@src/workers/geometryWorker.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Instrument Worker Pool</name>
  <files>src/lib/WorkerPool.ts</files>
  <action>
    Add structured console.debug and console.error calls to:
    1. Worker creation and termination.
    2. Job submission and queue status updates.
    3. Start/Completion of jobs with duration metrics.
    4. Worker crash detection and error details.
  </action>
  <verify>
    <automated>grep "console.debug" src/lib/WorkerPool.ts</automated>
  </verify>
  <done>WorkerPool is instrumented with metrics and lifecycle logs.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument Geometry Worker</name>
  <files>src/workers/geometryWorker.ts</files>
  <action>
    Add logging for message receipt, dimension validation, and mesh generation performance.
    Log specific warnings when dimensions hit edge cases (e.g., zero or extreme ratios).
  </action>
  <verify>
    <automated>grep "console.debug" src/workers/geometryWorker.ts</automated>
  </verify>
  <done>Geometry worker provides detailed generation traces.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument Backend CAD API</name>
  <files>backend/app/api/cad.py</files>
  <action>
    Add standard Python logging to the `analyze_geometry` endpoint.
    Log input dimensions and the result of the validation/analysis step.
  </action>
  <verify>
    <automated>grep "logger" backend/app/api/cad.py</automated>
  </verify>
  <done>Backend API logs CAD analysis details.</done>
</task>
</tasks>

<verification>
Start the application and check the browser console for structured [WorkerPool] and [GeometryWorker] logs during dimension changes.
</verification>

<success_criteria>

- [ ] WorkerPool logs job lifecycle.
- [ ] GeometryWorker logs dimension details.
- [ ] Backend logs analysis parameters.
      </success_criteria>

<output>
After completion, create `.planning/phases/05-debugging-testing/05-01-SUMMARY.md`
</output>
