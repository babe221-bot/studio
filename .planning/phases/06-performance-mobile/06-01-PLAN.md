---
phase: 06-performance-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [src/components/Lab.tsx, src/lib/WorkerPool.ts, next.config.js, package.json]
autonomous: true
requirements: [DEV-06, OPS-05]
must_haves:
  truths:
    - 'Page loads under 3 seconds on 3G connection'
    - 'Configurator is usable on mobile devices (320px-768px)'
    - 'CAD preview renders within 500ms for cached geometries'
  artifacts:
    - path: next.config.js
      provides: 'Performance optimizations (bundle splitting, image optimization)'
    - path: src/components/Lab.tsx
      provides: 'Responsive layout for mobile devices'
---

<objective>
Implement performance optimizations and responsive mobile design for the configurator.

Purpose: Ensure the platform performs well on slower connections and is accessible on tablets and phones.
Output: Optimized bundle sizes, lazy-loaded components, and mobile-responsive UI.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Lab.tsx
@next.config.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Code Splitting</name>
  <files>next.config.js</files>
  <action>
    Configure Next.js for optimal bundle splitting:
    1. Enable dynamic imports for heavy components (Three.js canvas, PDF generator)
    2. Configure lazy loading for non-critical routes
    3. Add webpack bundle analyzer to track sizes
  </action>
  <verify>
    <automated>grep "dynamic" next.config.js</automated>
  </verify>
  <done>Bundle analyzer shows reduced initial load size.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Images and Assets</name>
  <files>next.config.js, src/components</files>
  <action>
    1. Add next/image with proper sizing and WebP conversion
    2. Implement lazy loading for stone texture previews
    3. Configure cache headers for static assets
  </action>
  <verify>
    <automated>grep "next/image" src/components</automated>
  </verify>
  <done>Images load progressively with placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Mobile Responsive Layout</name>
  <files>src/components/Lab.tsx</files>
  <action>
    1. Add responsive breakpoints (mobile: 320px, tablet: 768px)
    2. Convert sidebar to bottom sheet or drawer on mobile
    3. Make touch targets minimum 44px
    4. Enable pinch-zoom for CAD preview on mobile
  </action>
  <verify>
    <automated>grep "media" src/components/Lab.tsx</automated>
  </verify>
  <done>Configurator is usable on mobile viewport.</done>
</task>

<task type="auto">
  <name>Task 4: Optimize Web Worker Pool</name>
  <files>src/lib/WorkerPool.ts</files>
  <action>
    1. Implement worker pre-warming for common configurations
    2. Add message batching for rapid dimension changes
    3. Cache geometry results indexed by dimensions
  </action>
  <verify>
    <automated>grep "cache" src/lib/WorkerPool.ts</automated>
  </verify>
  <done>Repeated configurations render faster.</done>
</task>
</tasks>

<verification>
1. Run Lighthouse audit - performance score > 80
2. Test configurator on mobile viewport (375px width)
3. Measure render time for cached vs new configurations
</verification>

<success_criteria>

- [ ] Bundle size reduced by 30% through code splitting
- [ ] Mobile layout is usable without horizontal scroll
- [ ] Cached geometries render under 500ms
- [ ] Lighthouse performance score > 80
      </success_criteria>

<output>
After completion, create `.planning/phases/06-performance-mobile/06-01-SUMMARY.md`
</output>
