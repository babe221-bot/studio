---
phase: 08-analytics-reporting
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/schemas.py
  - backend/app/api/admin/audit.py
  - backend/app/main.py
  - backend/app/api/admin/materials.py
  - backend/app/api/admin/orders.py
  - src/app/admin/audit/page.tsx
  - src/components/admin/Sidebar.tsx
autonomous: true
requirements:
  - ANALYTICS-04
must_haves:
  truths:
    - 'Admin can view an audit log table.'
    - 'Admin actions (updates/deletes) insert into AuditLogDB.'
  artifacts:
    - path: 'backend/app/api/admin/audit.py'
      provides: 'GET /api/admin/audit'
    - path: 'src/app/admin/audit/page.tsx'
      provides: 'Audit log table view.'
  key_links:
    - from: 'src/app/admin/audit/page.tsx'
      to: '/api/admin/audit'
      via: 'fetch audit logs'
---

<objective>
Implement detailed audit trails for administrative oversight to track who changed what and when.
Purpose: Provide accountability and historical tracing for critical business changes (pricing, materials, orders).
Output: Backend hooks generating logs and an Admin frontend log viewer.
</objective>

<context>
@backend/app/models/domain.py
@backend/app/api/admin/materials.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Audit schema and endpoint</name>
  <files>
    backend/app/models/schemas.py
    backend/app/api/admin/audit.py
    backend/app/main.py
  </files>
  <action>Add `AdminAuditLogResponse` and `AdminAuditLogCreate` schemas in `schemas.py` corresponding to `AdminAuditLogDB` in `domain.py`. Create `GET /` router in `audit.py` with pagination (`limit`, `offset`) to list logs ordered by `id` descending. Import and register the router under `/api/admin/audit` in `main.py`.</action>
  <verify>
    <automated>grep "/api/admin/audit" backend/app/main.py</automated>
  </verify>
  <done>Audit endpoints available.</done>
</task>

<task type="auto">
  <name>Task 2: Inject audit logging into existing mutations</name>
  <files>
    backend/app/api/admin/materials.py
    backend/app/api/admin/orders.py
  </files>
  <action>In `POST` and `PUT` endpoints (e.g., `update_material` and `update_order_status`), instantiate an `AdminAuditLogDB` object capturing `admin_id` (from auth), `action` (e.g., "UPDATE_MATERIAL"), `resource_type`, `resource_id`, and stringified `old_value`/`new_value` if easily available. Add `db.add(audit_log)` alongside the existing commits.</action>
  <verify>
    <automated>grep "AdminAuditLogDB" backend/app/api/admin/materials.py</automated>
  </verify>
  <done>Audit logs generated upon material or order modifications.</done>
</task>

<task type="auto">
  <name>Task 3: Create Audit Log UI page</name>
  <files>
    src/app/admin/audit/page.tsx
    src/components/admin/Sidebar.tsx
  </files>
  <action>Create a new Next.js route `/admin/audit/page.tsx` displaying a paginated `<Table>` of audit logs (Date, Admin ID, Action, Resource Type, Resource ID). Update the Sidebar links in `src/components/admin/Sidebar.tsx` to include an "Audit Logs" navigation item pointing to `/admin/audit`.</action>
  <verify>
    <automated>grep "/admin/audit" src/components/admin/Sidebar.tsx</automated>
  </verify>
  <done>Admin has a navigatable page to view recent actions.</done>
</task>
</tasks>

<success_criteria>
Any update to an order or material is recorded in the audit log database, and the admin can view a paginated list of these events on a dedicated page.
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics-reporting/08-04-SUMMARY.md`
</output>
