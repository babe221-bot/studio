---
phase: 08-analytics-reporting
plan: 02
type: execute
wave: 2
depends_on: ['08-01']
files_modified:
  - backend/app/models/domain.py
  - backend/app/models/schemas.py
  - backend/app/api/admin/widgets.py
  - src/components/admin/CustomWidgetsManager.tsx
  - src/app/admin/analytics/page.tsx
autonomous: true
requirements:
  - ANALYTICS-02
must_haves:
  truths:
    - 'Admin can create a custom widget configuration.'
    - 'Custom widgets are persisted across sessions.'
    - 'Widgets fetch data from their configured metric.'
  artifacts:
    - path: 'backend/app/models/domain.py'
      provides: 'AdminWidgetDB SQLAlchemy model.'
    - path: 'backend/app/api/admin/widgets.py'
      provides: 'CRUD API for widgets.'
    - path: 'src/components/admin/CustomWidgetsManager.tsx'
      provides: 'UI to create and render widgets.'
  key_links:
    - from: 'src/components/admin/CustomWidgetsManager.tsx'
      to: '/api/admin/widgets'
      via: 'fetch custom widgets config'
---

<objective>
Allow administrators to create custom analytics views (widgets) that display metrics of their choice.
Purpose: Provide flexibility for admins to track specific KPIs or custom queries over time.
Output: DB Model, CRUD Endpoints, and a Widget Manager UI.
</objective>

<context>
@src/app/admin/analytics/page.tsx
@backend/app/api/admin/analytics.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add widget schema to models</name>
  <files>
    backend/app/models/domain.py
    backend/app/models/schemas.py
  </files>
  <action>Create `AdminWidgetDB` in `domain.py` with columns (id: int pk, admin_id: str, title: str, metric_type: str, chart_type: str, config: json). Add matching Pydantic schemas in `schemas.py` (`AdminWidgetCreate`, `AdminWidgetResponse`).</action>
  <verify>
    <automated>grep -A 5 "class AdminWidgetDB" backend/app/models/domain.py</automated>
  </verify>
  <done>Model defined in SQLAlchemy and Pydantic.</done>
</task>

<task type="auto">
  <name>Task 2: Create widget CRUD endpoints</name>
  <files>backend/app/api/admin/widgets.py</files>
  <action>Create a new router for `/widgets`. Implement `GET /` (list for current admin), `POST /` (create), and `DELETE /{id}` (remove). Import router into `backend/app/main.py` if needed, or add it to `backend/app/api/admin/__init__.py`. Return real data from `AdminWidgetDB`.</action>
  <verify>
    <automated>grep "def create_widget" backend/app/api/admin/widgets.py</automated>
  </verify>
  <done>CRUD endpoints available.</done>
</task>

<task type="auto">
  <name>Task 3: Add Widget endpoints to main router</name>
  <files>backend/app/main.py</files>
  <action>Import the new `widgets` router from `backend.app.api.admin` and add it to the FastAPI app using `app.include_router(widgets.router, prefix="/api/admin/widgets", tags=["Admin Widgets"])`.</action>
  <verify>
    <automated>grep "/api/admin/widgets" backend/app/main.py</automated>
  </verify>
  <done>Endpoints registered.</done>
</task>

<task type="auto">
  <name>Task 4: Create Custom Widgets UI component</name>
  <files>src/components/admin/CustomWidgetsManager.tsx</files>
  <action>Build a React component that fetches the configured widgets. It should render a grid of custom charts/cards based on `metric_type` and `chart_type`. Include a "Add Widget" button that opens a dialog to pick `title`, `metric_type` (e.g., 'orders_count', 'revenue'), and `chart_type` (e.g., 'number', 'line', 'bar'), then POSTs to the backend.</action>
  <verify>
    <automated>cat src/components/admin/CustomWidgetsManager.tsx | grep "Add Widget"</automated>
  </verify>
  <done>Widget manager handles creation and rendering.</done>
</task>

<task type="auto">
  <name>Task 5: Integrate Widget Manager into Analytics Page</name>
  <files>src/app/admin/analytics/page.tsx</files>
  <action>Import and place `<CustomWidgetsManager />` below the standard static dashboard widgets on the analytics page.</action>
  <verify>
    <automated>grep "CustomWidgetsManager" src/app/admin/analytics/page.tsx</automated>
  </verify>
  <done>Custom widgets shown on the dashboard.</done>
</task>
</tasks>

<success_criteria>
Admins can dynamically create, view, and delete custom analytics widgets, displaying their configuration properly on the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics-reporting/08-02-SUMMARY.md`
</output>
