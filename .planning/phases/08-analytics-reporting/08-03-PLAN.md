---
phase: 08-analytics-reporting
plan: 03
type: execute
wave: 3
depends_on: ['08-02']
files_modified:
  - backend/app/api/admin/forecast.py
  - backend/app/main.py
  - src/components/admin/ForecastChart.tsx
  - src/app/admin/analytics/page.tsx
autonomous: true
requirements:
  - ANALYTICS-03
must_haves:
  truths:
    - 'Admin can view predictive charts for order trends.'
    - 'Forecast algorithm uses historical data to generate future values.'
  artifacts:
    - path: 'backend/app/api/admin/forecast.py'
      provides: 'GET /api/admin/forecast'
    - path: 'src/components/admin/ForecastChart.tsx'
      provides: 'UI chart mapping predictions.'
  key_links:
    - from: 'src/components/admin/ForecastChart.tsx'
      to: '/api/admin/forecast'
      via: 'fetch predictions'
---

<objective>
Implement forecasting for material usage and order trends based on historical data using simple linear regression or moving average.
Purpose: Enable predictive analytics for better resource planning and inventory management.
Output: Forecasting endpoint returning future timeline data and a new UI widget to display the forecast.
</objective>

<context>
@backend/app/api/admin/analytics.py
@src/app/admin/analytics/page.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add forecasting endpoint in python</name>
  <files>
    backend/app/api/admin/forecast.py
    backend/app/main.py
  </files>
  <action>Create a new router for `/forecast`. Implement `GET /` which takes a `metric` (e.g., 'orders' or 'revenue'). Query historical data (e.g., daily totals for the last 30 days) from the DB. Use simple numpy math (linear regression slope/intercept or moving average) to predict values for the next 7 or 14 days. Return `[{"date": "...", "actual": ..., "predicted": ...}, ...]`. Register the router in `main.py` under `/api/admin/forecast`.</action>
  <verify>
    <automated>grep "/api/admin/forecast" backend/app/main.py</automated>
  </verify>
  <done>Endpoint registered and returning time-series prediction arrays.</done>
</task>

<task type="auto">
  <name>Task 2: Create ForecastChart UI component</name>
  <files>src/components/admin/ForecastChart.tsx</files>
  <action>Build a React component fetching from `/api/admin/forecast?metric=orders`. Use Recharts to display a combined LineChart or ComposedChart where actual data is a solid line and predicted data is a dashed line. Ensure tooltips format the dates and values clearly.</action>
  <verify>
    <automated>cat src/components/admin/ForecastChart.tsx | grep "Recharts"</automated>
  </verify>
  <done>Forecast chart renders dual line series.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate ForecastChart on Analytics Page</name>
  <files>src/app/admin/analytics/page.tsx</files>
  <action>Import and place `<ForecastChart />` on the Analytics page next to the existing `RevenueChart` or in a new row dedicated to "Predictive Insights". Make sure to handle loading and error states cleanly.</action>
  <verify>
    <automated>grep "ForecastChart" src/app/admin/analytics/page.tsx</automated>
  </verify>
  <done>Forecast chart visible in the admin UI.</done>
</task>
</tasks>

<success_criteria>
Dashboard displays a chart with historical and predicted future trend lines for orders or revenue.
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics-reporting/08-03-SUMMARY.md`
</output>
