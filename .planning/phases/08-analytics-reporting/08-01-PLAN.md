---
phase: 08-analytics-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/api/admin/analytics.py
  - src/app/admin/analytics/page.tsx
  - src/components/admin/AnalyticsExportBtn.tsx
autonomous: true
requirements:
  - ANALYTICS-01
must_haves:
  truths:
    - 'Admin can click export and get CSV of current analytics.'
    - 'Admin can click export and get PDF of current analytics.'
  artifacts:
    - path: 'backend/app/api/admin/analytics.py'
      provides: 'GET /api/admin/analytics/export?format=csv|pdf'
    - path: 'src/components/admin/AnalyticsExportBtn.tsx'
      provides: 'Download button triggering API.'
  key_links:
    - from: 'src/components/admin/AnalyticsExportBtn.tsx'
      to: '/api/admin/analytics/export'
      via: 'fetch download'
---

<objective>
Add PDF and CSV export capabilities for analytics dashboard data.
Purpose: Enable admins to extract data for offline reporting and record-keeping.
Output: Backend export endpoint and frontend download button.
</objective>

<context>
@src/app/admin/analytics/page.tsx
@backend/app/api/admin/analytics.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add python dependencies for PDF export</name>
  <files>backend/requirements.txt</files>
  <action>Add `reportlab>=4.1.0` and `pandas>=2.2.0` to `backend/requirements.txt` to enable PDF and CSV generation and run `pip install -r backend/requirements.txt`.</action>
  <verify>
    <automated>grep -E "reportlab|pandas" backend/requirements.txt</automated>
  </verify>
  <done>Requirements updated</done>
</task>

<task type="auto">
  <name>Task 2: Create backend export endpoint</name>
  <files>backend/app/api/admin/analytics.py</files>
  <action>Add `GET /export` to the router. Accept `format` query param (`csv` or `pdf`). Fetch summary stats and revenue data, then format as CSV using pandas or csv module, or as PDF using reportlab. Return a StreamingResponse with appropriate headers (`Content-Disposition: attachment; filename=analytics.pdf`). Ensure endpoint checks admin auth using the same dependency as other analytics endpoints.</action>
  <verify>
    <automated>grep "def export_analytics" backend/app/api/admin/analytics.py</automated>
  </verify>
  <done>Endpoint returns formatted file stream.</done>
</task>

<task type="auto">
  <name>Task 3: Create AnalyticsExportBtn component</name>
  <files>src/components/admin/AnalyticsExportBtn.tsx</files>
  <action>Create a button component with a dropdown or simple modal to choose export format (CSV/PDF). On click, it performs a fetch to `/api/admin/analytics/export?format=...`, gets the blob, and triggers a browser download using URL.createObjectURL.</action>
  <verify>
    <automated>cat src/components/admin/AnalyticsExportBtn.tsx | grep "URL.createObjectURL"</automated>
  </verify>
  <done>Export button created.</done>
</task>

<task type="auto">
  <name>Task 4: Add export button to Analytics Page</name>
  <files>src/app/admin/analytics/page.tsx</files>
  <action>Import and place `AnalyticsExportBtn` in the header section of the Analytics page, next to the "Analytics Overview" title.</action>
  <verify>
    <automated>grep "AnalyticsExportBtn" src/app/admin/analytics/page.tsx</automated>
  </verify>
  <done>Button visible on the dashboard.</done>
</task>
</tasks>

<success_criteria>
Admin can click an export button and download the analytics summary in PDF and CSV format.
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics-reporting/08-01-SUMMARY.md`
</output>
