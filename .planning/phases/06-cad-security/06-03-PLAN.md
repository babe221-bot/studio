---
phase: 06-cad-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  [backend/app/api/cad.py, backend/app/main.py, src/lib/exports, src/components]
autonomous: true
requirements: [FEAT-05, OPS-08]
must_haves:
  truths:
    - 'Users can export configurations in multiple CAD formats'
    - 'API endpoints are protected by rate limiting'
    - 'Authentication tokens expire and refresh properly'
  artifacts:
    - path: backend/app/api/cad.py
      provides: 'Multi-format CAD export (STEP, OBJ, STL)'
    - path: backend/app/main.py
      provides: 'Rate limiting and security middleware'
---

<objective>
Add advanced CAD export formats and implement API security hardening.

Purpose: Provide more export options for CAD professionals and protect the platform from abuse.
Output: STEP/OBJ/STL export support and rate-limited API.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/api/cad.py
@backend/app/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement STEP Export</name>
  <files>backend/app/api/cad.py</files>
  <action>
    1. Add STEP file format support to CAD API
    - Use pythonocc or ezdxf library for STEP generation
    - Map slab geometry to STEP BREP representation
    2. Add /api/cad/export/step endpoint
    3. Include material properties in STEP metadata
  </action>
  <verify>
    <automated>grep "step" backend/app/api/cad.py</automated>
  </done>STEP export generates valid files.</done>
</task>

<task type="auto">
  <name>Task 2: Implement OBJ/STL Export</name>
  <files>backend/app/api/cad.py, src/lib/exports</files>
  <action>
    1. Add OBJ format with MTL material support
    2. Add STL format (binary and ASCII)
    3. Create frontend download handlers for each format
    4. Add format selection dropdown in UI
  </action>
  <verify>
    <automated>grep "obj\|stl" backend/app/api/cad.py</automated>
  </verify>
  <done>All three formats (STEP, OBJ, STL) export successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Implement API Rate Limiting</name>
  <files>backend/app/main.py</files>
  <action>
    1. Add slowapi for rate limiting
    - 100 requests/minute for authenticated users
    - 20 requests/minute for unauthenticated users
    2. Add rate limit headers (X-RateLimit-Limit, X-RateLimit-Remaining)
    3. Implement per-endpoint limits (stricter for CAD exports)
  </action>
  <verify>
    <automated>grep "RateLimit" backend/app/main.py</automated>
  </verify>
  <done>API returns 429 when limit exceeded.</done>
</task>

<task type="auto">
  <name>Task 4: Security Hardening</name>
  <files>backend/app/main.py, backend/app/api</files>
  <action>
    1. Implement JWT token refresh mechanism
    - Access token: 15 minute expiry
    - Refresh token: 7 day expiry with rotation
    2. Add CSRF protection for state-changing endpoints
    3. Implement request ID tracking for audit logging
    4. Add input sanitization for all API endpoints
  </action>
  <verify>
    <automated>grep "refresh" backend/app/api</automated>
  </verify>
  <done>Authentication flow includes proper token rotation.</done>
</task>
</tasks>

<verification>
1. Test each CAD export format opens in CAD software
2. Verify rate limiting triggers at threshold
3. Test token refresh flow
4. Verify CSRF protection blocks cross-site requests
</verification>

<success_criteria>

- [ ] STEP export produces valid STEP files
- [ ] OBJ/STL exports include material properties
- [ ] Rate limiting returns proper 429 responses
- [ ] Token refresh works without re-authentication
      </success_criteria>

<output>
After completion, create `.planning/phases/06-cad-security/06-03-SUMMARY.md`
</output>
